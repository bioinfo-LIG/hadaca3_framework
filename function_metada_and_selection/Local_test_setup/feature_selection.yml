
fsID :  #must be a unique ID without _
  path: function_blocks/feature_selection/ID.R
  short_name: FSID
  omic: [ANY]

# Toastnbfs :  #must be a unique ID without _
#   path: function_blocks/feature_selection/TOAST_nbfs.R
#   short_name: TNBF
#   omic: [mixMET,MET]

# Toastpercent :  #must be a unique ID without _
#   path: function_blocks/feature_selection/TOAST_percent.R
#   short_name: Tper
#   omic: [mixMET,MET]

# Toastvst :  #must be a unique ID without _
#   path: function_blocks/feature_selection/TOAST_vst.R
#   short_name: Tvst
#   omic: [mixRNA,RNA,scRNA]

# Toastbulknbfs : #TOTEST  #must be a unique ID withou
#   path: function_blocks/feature_selection/TOAST_bulk_nbfs.R
#   short_name: Tbnf
#   omic: [mixRNA,RNA,scRNA]

SCcluster :
 path: function_blocks/feature_selection/sc_cluster.R
 short_name: SCCL
#  need: [ref_cluster]
#  omic_need: [scRNA]
 omic: [mixRNA,RNA,scRNA]

# Toastvst :  #must be a unique ID without _
#  path: function_blocks/feature_selection/TOAST_vst.R
#  short_name: Tvst
#  omic: [mixRNA,RNA,scRNA]

# SPLSDA : #must be a unique ID without _
#   path: function_blocks/feature_selection/SPLS-DA.R
#   short_name: SPLS
#   omic: [mixMET,MET]

maxdiscriminant : # Use original ref_met to get max discriminant #must be a unique ID without _
  path: function_blocks/feature_selection/max_discriminant.R
  short_name: Mdis
  omic_need: [scRNA]
  omic: [mixMET,MET,]
